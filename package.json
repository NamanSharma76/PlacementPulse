{
  "name": "placementpulse-root",
  "version": "1.0.0",
  "description": "PlacementPulse Root Command Handler",
  "scripts": {
    "install:all": "npm install --prefix Backend && npm install --prefix Frontend",
    "dev:backend": "npm run dev --prefix Backend",
    "dev:frontend": "npm run dev --prefix Frontend",
    "dev": "npx -y concurrently -n \"Backend,Frontend\" -c \"blue,green\" \"npm run dev --prefix Backend\" \"npm run dev --prefix Frontend\"",
    "test:pdf": "node Backend/scratch/test-pdf-parser.js"
  }
}
